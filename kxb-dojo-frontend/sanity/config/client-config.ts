// Config used when creating Sanity client in sanity-utils.ts

const config = {
  projectId: "hir8lmw6",
  dataset: "production",
  apiVersion: "2023-07-26",
  useCdn: true,
};

export default config;
